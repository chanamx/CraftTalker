import { getHost } from '../host.js'

export const ARGUMENT_TYPE = getHost().ARGUMENT_TYPE
export const SlashCommandArgument = getHost().SlashCommandArgument
export const SlashCommandNamedArgument = getHost().SlashCommandNamedArgument
export default SlashCommandArgument
